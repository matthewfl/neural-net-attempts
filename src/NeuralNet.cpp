#include "NeuralNet.h"

using namespace std;

NeuralNet::Net::Net () {
  
}

NeuralNet::Net::Net(string data) {

}


NeuralNet::Net::~Net () {
  
}


string NeuralNet::Net::save () {
  
}


vector<int> run (vector<int> input) {
  for(int i=0;i<first.size() && i<input.size();i++) {
    
  }
}
